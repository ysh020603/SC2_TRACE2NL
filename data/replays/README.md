# data/replays

Inbox for pending `.SC2Replay` files.

Organize inputs by mirrored category subfolders (`human_ladder`, `human_tournament`, `sc2reader_official`).
Binary replay files are gitignored; folder READMEs and `.gitkeep` are tracked.
