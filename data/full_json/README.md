# data/full_json

Complete match JSON exports only.

Each category mirrors `data/replays/<category>/` and `data/artifacts/<category>/`.
Generated JSON payloads are gitignored; keep README / `.gitkeep`.
