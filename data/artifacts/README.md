# data/artifacts

Parse intermediates and reports (parquet, summary JSON, errors, previews, markdown samples).

Full match JSON does **not** belong here; write those under `data/full_json/<category>/`.
